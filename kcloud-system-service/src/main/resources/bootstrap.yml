server:
  port: 9001
spring:
  application:
    name: kcloud-system-service
  profiles:
    active: ${config.profile:dev}
  cloud:
    config:
      uri: http://localhost:8769
      fail-fast: false




