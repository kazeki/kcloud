spring:
  application:
    name: kcloud-home
  cloud:
    config:
      uri: http://localhost:${config.port:8888}
      name: ${config.name:${spring.application.name}}
      profile: ${config.profile:dev}
      label: ${config.label:master}